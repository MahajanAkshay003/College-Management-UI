import { createMuiTheme } from '@material-ui/core/styles';
import {indigo, green, pink} from '@material-ui/core/colors';

export default createMuiTheme({
    palette: {
        primary: indigo,
        secondary: pink,
        success: green
    }
});